# Archivo: teams/apps.py
# Etiqueta: Configuración de la app Teams

from django.apps import AppConfig

class TeamsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'teams'
